[buildout]
extends = buildout/appserver.cfg
develop = .

[config]
appserver_root_url = {{approot_url}}
fs_theme_path = ${buildout:directory}/themes/{{theme_name}}
test_submission_secret = {{test_submission_secret}}
