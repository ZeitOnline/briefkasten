<!DOCTYPE html>
<html lang="en"
  xmlns:tal="http://xml.zope.org/namespaces/tal"
  i18n:domain="briefkasten"
  xmlns:metal="http://xml.zope.org/namespaces/metal"
  metal:use-macro="master">
  <body>
    <tal:content metal:fill-slot="content">
        <form
            id="briefkasten-form"
            method="POST"
            role="form"
            enctype="multipart/form-data"
            accept-charset="utf-8"
            data-fileupload_url='${fileupload_url}'
            action="${action}">
            <label for="message">Anonymisierte Nachricht an die Redaktion</label>
            <textarea id="message" name="message" rows="10" cols="60"></textarea>
            <tal:block condition="request.registry.settings.get('briefkasten_flavor','briefkasten') == 'briefkasten'">
              <p>Datei anh&auml;ngen</p>
                <input type="file" name="upload">
                <label for="submit">Beim Abschicken wird die hochgeladene Datei zusammen mit Ihrer Textnachricht bereinigt und anonymisiert an die Redaktion uebermittelt.</label>
            </tal:block>
            <input type="hidden" name="testing_secret" value="" />
            <div tal:condition="request.registry.settings.get('briefkasten_flavor','briefkasten') == 'kummerkasten'">
            <input type="checkbox" name="want_feedback">Wuenschen sie einen Rueckkanal?</input>
            <p>Empfaenger auswaehlen</p>
            <div tal:repeat="editor editors">
                <input type="checkbox" checked name="recipients" value="${editor}">${editor}</input>
            </div>
            </div>
            <button id="briefkasten-formsubmit" name="submit" type="submit">Abschicken</button>
        </form>
    </tal:content>
  </body>
</html>
