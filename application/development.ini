# vi:syntax=dosini
[filter:paste_prefix]
use = egg:PasteDeploy#prefix

[app:briefkasten]
use = egg:briefkasten
filter-with = paste_prefix
pyramid.reload_templates = true
pyramid.debug_authorization = true
pyramid.debug_notfound = true
pyramid.debug_routematch = true
pyramid.debug_templates = true
pyramid.default_locale_name = de
pyramid.includes = 
fs_dropbox_root = var/drop_root/
fs_bin_path = middleware_scripts
post_secret = blubberlutschlimoschmecktoll
# appserver_root_url *must* end with trailing slash!
appserver_root_url = /
debug = True
briefkasten_flavour = kummerkasten

[server:main]
use = egg:pyramid#wsgiref
host = 0.0.0.0
port = 6543

[composite:main]
use = egg:Paste#urlmap
/assets = assets
/raw = raw
/ = default

[app:bootstrap-fonts]
# include a custom mapping for fonts, so bootstrap's hardcoded path works
use = egg:Paste#static
document_root = themes/fileuploader/fonts

[app:assets]
use = egg:Paste#static
# document_root = themes/default/assets
document_root = ../deployment/etc/theme/assets

[pipeline:default]
pipeline = theme
   briefkasten

[pipeline:raw]
pipeline = briefkasten

[filter:theme]
use = egg:diazo
#rules = themes/default/rules.xml
rules = ../deployment/etc/theme/rules.xml
debug = true
doctype = <!DOCTYPE html>
